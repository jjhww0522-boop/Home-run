name: home_run
description: "내집마련 목표 달성 앱 - 홈런 🏠"
publish_to: 'none'
version: 1.3.1+23

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # 상태 관리
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # 로컬 DB
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1

  # 라우팅
  go_router: ^14.6.2

  # 파일 시스템
  path_provider: ^2.1.5

  # 웹 저장소
  shared_preferences: ^2.3.4

  # 유틸리티
  intl: ^0.20.2
  uuid: ^4.5.1
  equatable: ^2.0.7
  dartz: ^0.10.1

  # UI
  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.2
  shimmer: ^3.0.0

  # 지도
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # HTTP (국토교통부 API용)
  dio: ^5.7.0

  # 환경 변수
  flutter_dotenv: ^5.2.1

  # XML 파싱
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # 코드 생성
  build_runner: ^2.4.13
  riverpod_generator: ^2.4.0
  isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - .env

  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/public/static/Pretendard-Regular.otf
          weight: 400
        - asset: assets/fonts/public/static/Pretendard-Medium.otf
          weight: 500
        - asset: assets/fonts/public/static/Pretendard-SemiBold.otf
          weight: 600
        - asset: assets/fonts/public/static/Pretendard-Bold.otf
          weight: 700
